To run the code you will have to specify the path to the folder using the terminal command "cd"
Next you will have to use to commands "javac" and "java" to make the code run.
Ignore all the "warnings" and "errors" that the program might show you, it's perfectly working ^^